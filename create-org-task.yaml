---
- name: Create an org list




#  {% for host in groups['app_servers'] %}
#  {{ hostvars[host]['ansible_facts']['eth0']['ipv4']['address'] }}
#  {% endfor %}

#  {% if 'webserver' in group_names %}
## some part of a configuration file that only applies to webservers
#  {% endif %}